# We will need to import:
#   - Column, Integer, and String from sqlalchemy
#   - declarative_base from sqlalchemy.orm https://docs.sqlalchemy.org/en/14/orm/mapping_api.html#sqlalchemy.orm.declarative_base
    
# Define the Base class

# Define a User class and inherit from Base

# Define the table name using __tablename__

# Add columns, id and username

# generate migrations with alembic

# Run the migration and check DB

# Populate DB by running seed file
